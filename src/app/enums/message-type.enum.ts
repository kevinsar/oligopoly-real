export enum MessageType {
  HOST = 'host',
  ALL = 'all',
  CONNECTED = 'connected',
  DISCONNECTED = 'disconnected',
  READY = 'ready',
  WAITING = 'waiting',
  GAME_STATE = 'game-state',
  ACTION = 'action'
}
